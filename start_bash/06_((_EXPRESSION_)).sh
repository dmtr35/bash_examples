#!/usr/bin/env bash

# EXPRESSION — арифметическое выражение.
# Арифметические операторы
# Оператор            Описание
# +                   Сложение
# -                   Вычитание
# *                   Умножение
# /                   Целочисленное деление
# **                  Степень числа
# %                   Деление по модулю (остаток от целочисленного деления)

# Арифметическое выражение считается истинным, если его результат отличается от нуля.

# можно использовать в if:
x=5
if (( x > 3 )); then
    echo "x больше 3"
fi

# ===============================================
# Присвоения и модификации
# Можно сразу присваивать:
(( x = 10 ))
(( y = x + 5 ))
(( x += 3 ))  # x = x + 3
(( y -= 2 ))  # y = y - 2
(( x *= 2 ))
(( y /= 3 ))
(( x %= 4 ))  # остаток от деления
# Инкременты и декременты:
(( x++ ))  # x = x + 1
(( ++x ))  # сначала x = x + 1, затем использует значение
(( y-- ))  # y = y - 1
(( --y ))  # сначала y = y - 1, затем использует значение

# ===============================================
a="77"
b="66"
# Арифметические операции (Присваивание через одинарный =)
# Можно использовать скобки для приоритетов: (( a + (b * c) ))
(( x = a + b ))
(( x = a - b ))
(( x = a * b ))
(( x = a / b ))
(( x = a % b ))
(( x = a ** b ))  # возведение в степень

# ===============================================
# Сравнения (Возвращают 1, если ложь, 0, если истина.)
a="11"
b="11"
c="13"
(( a == b )) && echo "равны"
(( a != c )) && echo "не равны"
(( a < c )) && echo "a меньше b"
(( a <= b )) && echo "a меньше или равно b"
(( a > b )) && echo "a больше b"
(( a >= b )) && echo "a больше или b"

if (( x != 0 )); then
    echo "x не равен нулю"
fi
# ===============================================

# Логические операции
a="11"
b="11"
(( a && b ))        # логическое И
(( a || b ))        # логическое ИЛИ
(( !a ))            # логическое НЕ

# ===============================================
# Побитовые операции
(( a & b ))         # AND
(( a | b ))         # OR
(( a ^ b ))         # XOR
(( ~a ))            # NOT
(( res = 2 << 1 ))        # сдвиг влево на 2
(( a >> 1 ))        # сдвиг вправо на 1

# ===============================================
# Особенности
# Нет необходимости писать $ перед переменной:
x=3
(( x = x + 2 ))  # верно

# Можно использовать в условных выражениях, циклах, присвоениях.
# Результат выражения можно получить с помощью $(( )):
result=$(( x + y ))

# ===============================================

# Для группировки подвыражений допускается использование
# одиночных круг­лых скобок при этом будет
# использоваться одна операция подстановки вместо двух:
echo $(($((5**2)) * 3))
echo $(((5**2) * 3))
# ===============================================
 
# целочисленное деление:
echo Пять разделить на два будет $(( 5 / 2 ))
# Пять разделить на два будет 2
echo и $(( 5 % 2 )) в остатке.
# и 1 в остатке.


# ===============================================
# Определение основания системы счисления
# Число           - По умолчанию как десятичные числа (в системе счисления с основанием 10)
# 0число          - В арифметических выражениях числа, начинающиеся с нуля, как восьмеричные (в системе счисления с основанием 8)
# 0xчисло         - Форма записи шестнадцатеричных чисел
# основание#число - Число в системе счисления с указанным основанием
echo $((0xff))                      # > 255
echo $((2#11111111))                # > 255

# ===============================================
# Операторы присваивания
# параметр = значение         Простое присваивание. Присваивает указанное значениеуказанному параметру
# параметр += значение        Присваивание со сложением. Эквивалентно выражениюпараметр = параметр + значение
# параметр -= значение        Присваивание с вычитанием. Эквивалентно выражениюпараметр = параметр - значение
# параметр *= значение        Присваивание с умножением. Эквивалентно выражениюпараметр = параметр × значение
# параметр /= значение        Присваивание с целочисленным делением. Эквивалентновыражению параметр = параметр ÷ значение
# параметр %= значение        Присваивание с делением по модулю. Эквивалентно выражению параметр = параметр % значение
# параметр++                  Постинкремент переменной. Эквивалентно выражению параметр = параметр + 1. (Но см. пояснение ниже.)
# параметр--                  Постдекремент переменной. Эквивалентно выражению параметр = параметр - 1
# ++параметр                  Преинкремент переменной. Эквивалентно выражению параметр = параметр + 1
# --параметр                  Предекремент переменной. Эквивалентно выражению параметр = параметр - 1

# ===============================================
# Битовые операторы
# Оператор                    Описание
# ~                           Поразрядное отрицание. Изменяет значения всех битов в числе на противоположные
# <<                          Поразрядный сдвиг влево. Сдвигает все биты в числе на один разряд влево
# >>                          Поразрядный сдвиг вправо. Сдвигает все биты в числе на один разряд вправо
# &                           Поразрядная операция И (AND). Выполняет операцию И над всеми битами двух чисел
# |                           Поразрядная операция ИЛИ (OR). Выполняет операцию ИЛИ над всеми битами двух чисел
# ^                           Поразрядная операция ИСКЛЮЧАЮЩЕЕ ИЛИ (XOR). Выполняет операцию ИСКЛЮЧАЮЩЕЕ ИЛИ над всеми битами двух чисел

# поразрядный сдвиг влево, вывести список степеней 2:
# for i in A B C D; do echo $i; done
# for (( i=0; i<8; ++i )); do echo $(( 1 << i )); done
# 1
# 2
# 4
# 8
# 16
# 32
# 64
# 128

# num=128
# echo $(( num << 1 ))        # 256
# echo $(( num >> 1 ))        # 64

# ===============================================
# Операторы сравнения (возвращающее 0, считается ложным, а выражение, возвращающее ненулевое значение, — истинным)
# Оператор                                  Описание
# <=                                        Меньше или равно
# >=                                        Больше или равно
# <                                         Меньше
# >                                         Больше
# ==                                        Равно
# !=                                        Не равно
# &&                                        Логическое И (AND)
# ||                                        Логическое ИЛИ (OR)
# выражение1?выражение2:выражение3          Тернарный оператор сравнения. (не работает со строками)

a=0
((a<1?++a:--a)) && echo $a              # > "1"
((a<1?(a+=1):(a-=1)))





exit 0